import {IDataBaseModel, IDatabaseGenerator} from '../../Interfaces'

export class SpringDatabaseGenerator implements IDatabaseGenerator {
	generate(model: IDataBaseModel): string {
		return 'Coming soon'
	}
}
